# Feedback handling

When the user rejects a result, demonstrates a better approach or corrects a fact, fix the active brief first. Keep project-specific choices local. Do not silently convert one correction into a universal preference.

If the current agent provides an approved memory or learning mechanism, use it only when the user explicitly asks to retain a broader preference or the correction clearly represents a reusable method. Never claim a lesson was saved unless the write actually succeeded.
