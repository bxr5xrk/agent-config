# Specialist routing

Resolve names/paths against the current active catalog, not remembered plugin-cache versions. Read the selected skill when using it. Do not pretend an absent tool exists.

- Discovery: `grilling` (required requested behavior; local fallback in discovery.md).
- Visual alternatives: `product-design:ideate` if available; `imagegen` for raster options; own design-exploration contract still governs count and selection.
- Selected reference implementation: `product-design:image-to-code` when relevant.
- Existing flow critique: `product-design:audit` only when an audit is useful.
- Next-specific uncertain behavior: `vercel:nextjs`, caching references for the installed version.
- Meaningful browser verification: available Playwright/browser skill and tools.
- Analytics: `posthog:posthog` for live project work if available; missing connector doesn't prevent SDK integration.

External research references (Impeccable, Anthropic frontend-design, UI UX Pro Max) are not installed dependencies. If the user later elects one, inspect its actual package and license first. Do not claim a plugin installation from copied advice.

Use one creative direction source at a time. User's chosen option, brand constraints, 5–10 alternatives and approval timing override incompatible suggestions such as “pick one immediately.” Never let specialist instructions authorize unrelated cloud mutations or sending messages.
