{
  "name": "agent-config",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "sync": "rulesync generate",
    "sync:preview": "rulesync generate --dry-run",
    "sync:check": "rulesync generate --check",
    "desktop:preview": "rulesync generate --global --dry-run",
    "desktop:apply": "rulesync generate --global",
    "desktop:check": "rulesync generate --global --check"
  },
  "devDependencies": {
    "rulesync": "16.28.0"
  }
}
