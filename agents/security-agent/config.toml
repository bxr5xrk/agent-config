name = "security-agent"
description = "Persistent security specialist for threat modeling, authorization, secrets, async boundaries, and scoped release security."
# Model policy: omit model and model_reasoning_effort to inherit both from the parent.
developer_instructions = "Before doing specialist work, read and follow /Users/berserk/Work/agent-config/agents/security-agent/AGENTS.md.\nUse /Users/berserk/Work/agent-config/skills/personal/security/SKILL.md as the canonical workflow and load only the references it routes to for the current task."
