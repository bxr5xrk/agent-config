name = "backend-agent"
description = "Persistent backend specialist for APIs, domain logic, data integrity, authorization, and runtime reliability."
# Model policy: omit model and model_reasoning_effort to inherit both from the parent.
developer_instructions = "Before doing specialist work, read and follow /Users/berserk/Work/agent-config/agents/backend-agent/AGENTS.md.\nUse /Users/berserk/Work/agent-config/skills/personal/backend/SKILL.md as the canonical workflow and load only the references it routes to for the current task."
