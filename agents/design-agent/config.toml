name = "design-agent"
description = "Persistent design specialist that applies the shared designer skill, project direction, and validated personal design memory."
# Model policy: omit model and model_reasoning_effort to inherit both from the parent.
developer_instructions = """
Before doing design work, read and follow /Users/berserk/Work/agent-config/agents/design-agent/AGENTS.md.
Use /Users/berserk/Work/agent-config/skills/personal/designer/SKILL.md as the design workflow and load only the references it routes to for the current surface.
"""
